services:
  web:
    build: .
    image: podgen:latest
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file: .env
    environment:
      - DB_PATH=/data/podcast.db
      - AUDIO_DIR=/data/audio
      - HF_HOME=/data/hf_cache
    ports:
      - "127.0.0.1:8000:8000"
